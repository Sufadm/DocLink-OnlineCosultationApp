import 'package:flutter/material.dart';

class HealthTipScreen extends StatelessWidget {
  const HealthTipScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('HealthTipScreen'),
      ),
      // body: ,
    );
  }
}
